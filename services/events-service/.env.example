# HTTP
PORT=4003
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info

# Auth (HS256 sau RS256)
JWT_ALG=HS256
JWT_SHARED_SECRET=changeme_super_secret
# pentru RS256 (opțional, base64 al cheii publice, doar “n” dacă folosești JWK simplificat)
# JWT_PUBLIC_KEY_BASE64=

# DB (MySQL)
DATABASE_URL="mysql://root:123456789@localhost:3306/eventure_events_service"
# NATS (conform root)
NATS_URL=nats://localhost:4222
NATS_USER=
NATS_PASS=
NATS_PREFIX=events
